doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title Error
  body
    h1 Error
    if message
      h2= message
    if error && error.stack
      pre= error.stack
    else
      p An error occurred.
